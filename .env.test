# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=wattwatch_test
DB_SSL_MODE=disable

# API Configuration
API_PORT=8080 

# Auth
JWT_SECRET=test_secret_key
JWT_EXPIRATION_HOURS=24
REGISTRATION_OPEN=true

# Email
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=test
SMTP_PASSWORD=test
SMTP_FROM=test@example.com
APP_URL=http://localhost:8080 

ENABLE_NORDPOOL=true